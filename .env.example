# Copy to .env and set local values. Sentinel does not automatically load .env.
# Export these values into the launching process environment when needed.
TYPESAFE_API_KEY=
JEV_SENTINEL_ALLOW_REMOTE=0
JEV_SENTINEL_WEBHOOK_KEY=
